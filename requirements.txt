# The pipeline and the evaluation harness need NO third-party packages.
# Everything below is optional and the system degrades cleanly without it.

# Live model calls. Without this the system uses its deterministic
# classifier and extractive generator.
requests==2.31.0

# .env loading. src/config.py falls back to a small stdlib parser.
python-dotenv==1.0.0

# Tests.
pytest==7.4.0
